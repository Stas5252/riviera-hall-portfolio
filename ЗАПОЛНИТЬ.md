# Что нужно заполнить перед запуском

Все места, где сейчас стоят заглушки. В браузере они подсвечены розовым
(`[data-fill]` в `css/legal.css`) — как заполните, подсветка пропадёт.

## 1. Домен

Сейчас везде прописан `https://riviera-hall.ru`. Заменить на реальный в файлах:

| Файл | Что там |
|---|---|
| `robots.txt` | строка `Sitemap:` |
| `sitemap.xml` | все шесть адресов |
| `index.html` | `canonical`, `og:url`, `og:image`, оба блока JSON-LD |
| `gallery-*.html` | `canonical`, `og:url`, `og:image` |
| `privacy.html` | `canonical` и адреса в тексте |

Быстрая замена одной командой из папки сайта:

```bash
grep -rl "riviera-hall.ru" --include="*.html" --include="*.xml" --include="*.txt" . | xargs sed -i 's|riviera-hall\.ru|ВАШ-ДОМЕН.ru|g'
```

## 2. Данные ИП

| Что | Где |
|---|---|
| ФИО полностью | `index.html` подвал, `privacy.html` п.1 |
| ИНН | `index.html` подвал, `privacy.html` п.1 |
| ОГРНИП | `index.html` подвал, `privacy.html` п.1 |
| Адрес регистрации | `index.html` подвал, `privacy.html` п.1 |
| Email общий | `privacy.html` п.1 |
| Email для запросов по ПДн | `privacy.html` п.1 и п.8 |
| Хостинг-провайдер и его ИНН | `privacy.html` п.6 |
| Срок хранения заявок | `privacy.html` п.5 (сейчас 3 года) |
| Дата редакции политики | `privacy.html` заголовок |

## 3. Контакты площадки

| Что | Где |
|---|---|
| Телефон | `index.html`: меню (`.drawer__tel`), нижняя панель (`.dock__call`), футер (`.mgr__tel`), JSON-LD `telephone` |
| Имя менеджера | `index.html` — `.mgr__name` |
| Точный адрес | `index.html` — блок «09 — как проехать», JSON-LD `address` |
| Координаты | `index.html` — JSON-LD `geo` (широта/долгота с Яндекс.Карт) |
| Город | `index.html` — JSON-LD `addressLocality` |
| Ссылки на соцсети | `index.html` — `.mgr__links`, сейчас `href="#"` |
| Часы работы | `index.html` — JSON-LD `openingHoursSpecification`, сейчас 10:00–22:00 |

## 4. Приём заявок

Форма отправляет JSON на адрес из атрибута `data-endpoint` тега `<form>`.
Атрибут пока не задан — форма честно показывает ошибку и телефон вместо
ложного «Заявка отправлена».

Нужно: поднять обработчик на хостинге и прописать
`<form class="form" id="form" data-endpoint="/api/lead">`.

Формат, который уходит:

```json
{ "name":"…", "tel":"…", "agree":"on",
  "page":"https://…", "ts":"2026-08-11T…" }
```

Форма собирает только имя и телефон — остальное уточняется в разговоре.
Так требует ч. 5 ст. 5 152-ФЗ: объём данных не должен быть избыточным
относительно цели. Чем меньше собрано, тем меньше риск при утечке.

## 5. Яндекс.Метрика

В `js/main.js` вверху: `const METRIKA_ID = '';` — вписать номер счётчика.
Пока пусто, счётчик не грузится вообще, даже при согласии на cookie.

После подключения завести цель `form_sent` — она уже отправляется из кода
при успешной отправке заявки.

## 6. Контент, который врёт

| Что | Где | Почему нельзя оставить |
|---|---|---|
| Три отзыва | `index.html`, секция «10 — отзывы» | Площадка ещё не работала, отзывы вымышленные — ст. 5 закона о рекламе |
| «Открытие июль 2026» | `index.html`, `.hero__eyebrow` | Дата прошла |
| Карта-заглушка | `index.html`, `.route__stub` | Вставить `<iframe>` Яндекс.Карт |
| Видео | `index.html`, секция «08 — видео» | Сейчас фотография с кнопкой play, ролика нет |

## 7. Не на сайте

**Обязательно для ИП:**

- Уведомление оператора ПДн на `pd.rkn.gov.ru` — бесплатно, около 20 минут,
  в реестр включают за 30 дней. За неподачу штрафуют отдельно.
- Публикация политики обработки ПДн в открытом доступе — ч. 2 ст. 18.1 152-ФЗ.
  Эта обязанность у **всех** операторов, включая ИП. Уже сделано: `privacy.html`.
- Меры защиты по ст. 18.1 ч. 1 и ст. 19 — «необходимые и достаточные»,
  конкретный состав закон для ИП не расписывает.

**Не обязательно для ИП, но полезно:**

Приказ о назначении ответственного за организацию обработки и издание локальных
актов — это пп. 1 и 2 ч. 1 ст. 18.1, и они адресованы **операторам, являющимся
юридическими лицами**. На ИП формально не распространяются. Держать их всё равно
стоит: при проверке проще показать документ, чем объяснять устно.

**Прочее:**

- Журнал учёта обращений субъектов ПДн — пригодится, если придёт запрос.
- Подтверждение прав на сайт в Яндекс.Вебмастере, отправка `sitemap.xml`.
- Проверка политики и рекламных текстов юристом до запуска рекламы.
